{
  "name": "@maxigarcia/view-transitions-app",
  "type": "module",
  "version": "0.5.11",
  "private": true,
  "description": "Demo site for @maxigarcia/view-transitions — try View Transitions API theme animations in the browser.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "clean": "rm -rf .astro dist",
    "phoenix": "npm run clean && rm -rf node_modules"
  },
  "dependencies": {
    "@maxigarcia/view-transitions": "*",
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^6.3.7",
    "tailwindcss": "^4.3.0"
  }
}
